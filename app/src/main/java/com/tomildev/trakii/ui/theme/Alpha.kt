package com.tomildev.trakii.ui.theme

object Alpha {
    /** Used for secondary text, descriptions, or elements with less emphasis. */
    const val Secondary = 0.7f
    
    /** Used for disabled icons or hint states. */
    const val Hint = 0.5f
    
    /** Used for very subtle borders or dividers. */
    const val Divider = 0.12f
    
    /** Used for background overlays. */
    const val Overlay = 0.3f
    
    /** Full value (no transparency). */
    const val Full = 1.0f
}